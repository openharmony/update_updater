// 测试加减乘除
a = 3 + 56;
b =  5 + 6;
c = (a + b) * (a - b);
e = 0;

function computer(a, b, c) {
	if (a == b) {
		return a, c;
	} else if (a != c) {
		e = 100;
		return b, c;
	} else {
		return a, b;
	}
	return c, b;
}

Stdout("a,b,c = ", computer(2,3,2), e);

for (a = 0; a < 10; a = a + 1) {
	Stdout("********** ", a);
	if (a < 2) {
		Stdout("a = ", a + 100);
		continue;
	} else if (a < 4) {
		Stdout("a = ", a + 200);
	} else if (a < 6) {
		Stdout("a = ", a + 300);
	} else {
		Stdout("a = ", a + 400);
	}
	if (a >= 8) {
		break;
	}
}
Stdout("a = ", a);
  




